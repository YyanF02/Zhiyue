# 智阅二手书平台 · 部署指南

> AIVA 驱动的智能二手书交易系统

---

## 项目概览

智阅是一款基于 **Spring AI + Vue 3** 构建的二手书交易平台，集成 AI 智能助手「小阅」，提供流畅的书籍搜索、上架与购买体验。

### 核心功能

| 模块 | 功能 |
|------|------|
| AI 助手 | 多 Agent 路由、RAG 向量检索、Function Calling |
| 用户端 | 书籍浏览、搜索、购买、聊天、收藏、评价 |
| 管理端 | 商品管理、订单管理、用户管理 |
| 支付 | 支付宝沙箱支付 |

### 技术架构

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Vue 3     │────▶│ Spring Boot │────▶│   MySQL     │
│  (Vite)     │     │   3.5.5     │     │   8.0       │
└─────────────┘     └──────┬──────┘     └─────────────┘
                          │
              ┌───────────┼───────────┐
              ▼           ▼           ▼
        ┌─────────┐ ┌─────────┐ ┌─────────┐
        │  Redis  │ │ RabbitMQ│ │ Milvus  │
        │  7.2.0  │ │  3.x    │ │  2.x    │
        └─────────┘ └─────────┘ └─────────┘
```

---

## 一、环境准备

### 1.1 安装 Docker

确保已安装 Docker Desktop (Windows) 或 Docker Engine (Linux/macOS)。

```bash
docker --version
docker-compose --version
```

### 1.2 克隆项目

```bash
git clone <your-repo-url>
cd bookstore
```

---

## 二、Docker 部署服务

### 2.1 MySQL 8.0

```bash
docker run -d \
  --name mysql-zhiyue \
  -p 3306:3306 \
  -e MYSQL_ROOT_PASSWORD=<YOUR_PASSWORD> \
  -e MYSQL_DATABASE=ZhiyueSecondHand \
  -v mysql-data:/var/lib/mysql \
  -v $(pwd)/zhi-yue/ZhiyueSecondHand/src/main/resources/database_schema.sql:/docker-entrypoint-initdb.d/init.sql \
  --restart unless-stopped \
  mysql:8.0
```

**参数说明：**

| 参数 | 说明 |
|------|------|
| MYSQL_ROOT_PASSWORD | 设置 root 密码（需与 application-local.yaml 一致）|
| MYSQL_DATABASE | 数据库名 |
| 端口映射 | 3306:3306 |

### 2.2 Redis 7.2

```bash
docker run -d \
  --name redis-zhiyue \
  -p 6379:6379 \
  -e REDIS_PASSWORD=<YOUR_PASSWORD> \
  --restart unless-stopped \
  redis:7.2 \
  redis-server --requirepass <YOUR_PASSWORD>
```

### 2.3 RabbitMQ 3.x

```bash
docker run -d \
  --name rabbitmq-zhiyue \
  -p 5672:5672 \
  -p 15672:15672 \
  -e RABBITMQ_DEFAULT_USER=<YOUR_USER> \
  -e RABBITMQ_DEFAULT_PASS=<YOUR_PASSWORD> \
  --restart unless-stopped \
  rabbitmq:3-management
```

- Management UI: http://localhost:15672

### 2.4 MinIO (对象存储)

```bash
docker run -d \
  --name minio-zhiyue \
  -p 9100:9000 \
  -p 9101:9001 \
  -e MINIO_ROOT_USER=<YOUR_ACCESS_KEY> \
  -e MINIO_ROOT_PASSWORD=<YOUR_SECRET_KEY> \
  -v minio-data:/data \
  --restart unless-stopped \
  minio/minio:latest \
  server /data --console-address ":9001"
```

**初始化 MinIO 存储桶：**

1. 访问 http://localhost:9101
2. 登录后创建 bucket: `book`

### 2.5 Milvus 2.x (向量数据库)

**Milvus Lite（单机快速部署）**

```bash
docker run -d \
  --name milvus-zhiyue \
  -p 19530:19530 \
  -p 9091:9091 \
  -v milvus-data:/var/lib/milvus \
  --restart unless-stopped \
  milvusdb/milvus:v2.4.0 \
  milvus-server
```

**参数说明：**

| 参数 | 说明 |
|------|------|
| 端口 | 19530 |
| 数据库 | ap |
| 集合 | ZhiyueSecondHand_store |
| 向量维度 | 1024 |

---

## 三、后端配置

### 3.1 配置文件位置

```
zhi-yue/ZhiyueSecondHand/src/main/resources/
├── application.yaml          # 主配置（占位符）
└── application-local.yaml   # 本地配置（敏感信息勿外传）
```

### 3.2 application-local.yaml 模板

```yaml
spring:
  ai:
    # 通义千问 API Key
    my-api-key: <YOUR_DASHSCOPE_API_KEY>
    my-chat-model: qwen3.5-plus
    my-embedding-model: text-embedding-v4
    embedding:
      dimensions: 1024

  vectorstore:
    milvus:
      host: localhost
      port: 19530
      username: ''
      password: ''
      databaseName: ap
      collectionName: ZhiyueSecondHand_store

  datasource:
    password: <YOUR_MYSQL_PASSWORD>
    username: root
    url: jdbc:mysql://localhost:3306/ZhiyueSecondHand?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=convertToNull
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      password: <YOUR_REDIS_PASSWORD>

  rabbitmq:
    host: localhost
    port: 5672
    username: <YOUR_RABBITMQ_USER>
    password: <YOUR_RABBITMQ_PASSWORD>
    virtual-host: /

minio:
  secret-key: <YOUR_MINIO_SECRET>
  bucket: book
  read-path: http://localhost:9100
  access-key: <YOUR_MINIO_ACCESS_KEY>
  endpoint: http://localhost:9100

# 支付宝配置
alipay:
  server-url: https://openapi-sandbox.dl.alipaydev.com/gateway.do
  app-id: <YOUR_ALIPAY_APP_ID>
  private-key: <YOUR_ALIPAY_PRIVATE_KEY>
  alipay-public-key: <YOUR_ALIPAY_PUBLIC_KEY>

server:
  external-url: <YOUR_PUBLIC_BACKEND_URL>
  vue-url: <YOUR_PUBLIC_FRONTEND_URL>
```

### 3.3 配置项对照表

| 配置项 | 来源 | 说明 |
|--------|------|------|
| `spring.ai.my-api-key` | 阿里云 DashScope 控制台 | 必填 |
| `mysql.pw` | 你设置的 MySQL 密码 | 需同步 Docker |
| `redis.pw` | 你设置的 Redis 密码 | 需同步 Docker |
| `mq.un/pw` | 你设置的 RabbitMQ 账号 | 需同步 Docker |
| `minio.*` | MinIO 控制台 | 需同步 Docker |
| `alipay.*` | 支付宝开放平台 | 沙箱/正式均可 |
| `server.external-url` | cpolar/ngrok/公网域名 | AI 回调需要 |

### 3.4 启动后端

```bash
cd zhi-yue/ZhiyueSecondHand

# 打包
mvn clean package -DskipTests

# 运行
java -jar target/ZhiyueSecondHand-0.0.1-SNAPSHOT.jar
```

---

## 四、前端部署

### 4.1 环境要求

- Node.js 18+
- npm 9+

### 4.2 安装依赖

```bash
cd zhi-yue/ZhiyueFrontend
npm install
```

### 4.3 构建

```bash
npm run build
```

产物输出至 `dist/`

### 4.4 部署静态文件

**Nginx 配置示例：**

```nginx
server {
    listen 80;
    server_name your-domain.com;

    root /var/www/zhiyue/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass https://your-backend-domain.com;
    }
}
```

---

## 六、内网穿透配置

若部署在本地机器，需通过内网穿透让外部访问。以下是常用方案：

### 6.1 cpolar（推荐）

**安装 cpolar：**

```bash
# Windows 下载 https://cpolar.com/download
# 或通过 winget 安装
winget install xiaoluyy.Cpolar
```

**启动隧道：**

```bash
# 后端 API (8080 → 公网)
cpolar http 8080

# 前端 (5173 → 公网)
cpolar http 5173

# 保持后台运行 (认证后)
cpolar http 8080 -subdomain=your-backend --log=stdout &
cpolar http 5173 -subdomain=your-frontend --log=stdout &
```

**配置固定域名（认证后）：**

```bash
cpolar http 8080 -subdomain=your-backend -region=cn_vip
cpolar http 5173 -subdomain=your-frontend -region=cn_vip
```

### 6.2 ngrok

**安装 ngrok：**

```bash
# Windows: https://ngrok.com/download
# 或 npm install -g ngrok

ngrok http 8080
ngrok http 5173
```

### 6.3 内网穿透后的配置

隧道启动后，将获得的公网地址填入 `application-local.yaml`：

```yaml
server:
  # 你的后端 cpolar/ngrok 地址
  external-url: https://xxxxx.vip.cpolar.cn
  # 你的前端 cpolar/ngrok 地址
  vue-url: https://xxxxx.vip.cpolar.cn
```

**同时修改前端 API 地址：**

编辑 `zhi-yue/ZhiyueFrontend/src/config/index.js` 或创建 `.env.production`：

```javascript
VITE_API_BASE_URL = 'https://xxxxx.vip.cpolar.cn'
```

### 6.4 支付宝回调配置

沙箱环境下，回调地址必须公网可访问。在支付宝沙箱后台设置：

```
支付回调地址: https://xxxxx.vip.cpolar.cn/pay/notify
```

### 6.5 支付宝沙箱配置详解

#### 6.5.1 获取沙箱参数

1. 登录 [支付宝开放平台沙箱](https://openhome.alipay.com/develop/sandbox/app)
2. 获取以下参数：

| 参数 | 说明 | 获取位置 |
|------|------|----------|
| app-id | 应用唯一 ID | 沙箱应用信息页 |
| private-key | 应用私钥 | 需要自己生成 RSA2 密钥对 |
| alipay-public-key | 支付宝公钥 | 沙箱应用信息页 |

#### 6.5.2 生成 RSA2 密钥对

```bash
# 生成私钥 (PKCS#8 格式)
openssl genrsa -out app_private_key.pem 2048
openssl pkcs8 -topk8 -inform PEM -outform PEM -nocrypt -in app_private_key.pem -out app_private_key_pkcs8.pem

# 提取公钥
openssl rsa -in app_private_key.pem -pubout -out app_public_key.pem
```

将 `app_private_key_pkcs8.pem` 内容填入 `private-key`，
支付宝页面显示的公钥填入 `alipay-public-key`。

#### 6.5.3 沙箱配置示例

```yaml
alipay:
  # 沙箱网关（固定）
  server-url: https://openapi-sandbox.dl.alipaydev.com/gateway.do
  # 沙箱 App ID
  app-id: 2021000123456789
  # 私钥 (PKCS#8 格式，勿换行)
  private-key: MIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC...
  # 支付宝公钥
  alipay-public-key: MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA...
  format: json
  charset: UTF-8
  sign-type: RSA2
```

#### 6.5.4 正式环境切换

上线时替换为正式网关和正式 App：

```yaml
alipay:
  server-url: https://openapi.alipay.com/gateway.do
  app-id: <YOUR_OFFICIAL_APP_ID>
  private-key: <YOUR_OFFICIAL_PRIVATE_KEY>
  alipay-public-key: <YOUR_OFFICIAL_PUBLIC_KEY>
```

---

## 七、验证部署

### 7.1 检查服务状态

```bash
docker ps
```

### 7.2 访问服务

| 服务 | 地址 |
|------|------|
| 前端 (本地) | http://localhost:5173 |
| 后端 API (本地) | http://localhost:8080 |
| Swagger UI | http://localhost:8080/doc.html |
| MinIO Console | http://localhost:9101 |
| RabbitMQ | http://localhost:15672 |

### 7.3 穿透验证

```bash
# 确认隧道正常
curl https://xxxxx.vip.cpolar.cn/api/user/health
```

---

## 八、一键清理

```bash
docker stop mysql-zhiyue redis-zhiyue rabbitmq-zhiyue minio-zhiyue milvus-zhiyue
docker rm mysql-zhiyue redis-zhiyue rabbitmq-zhiyue minio-zhiyue milvus-zhiyue
docker volume rm mysql-data minio-data milvus-data
```

---

> 部署遇到问题？请检查日志 `docker logs <container-name>`
